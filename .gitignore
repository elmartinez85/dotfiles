# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon
._*

# Backup files created by bootstrap
*.backup

# Claude Code directory (optional - uncomment if you don't want to track)
# .claude/

# Temporary files
*.tmp
*.log

# Local SSH config (not tracked)
config/ssh_config.local

# Environment secrets (API keys, tokens, etc.)
.env_secrets

# Note: Brewfile, .profile_active, and config/aerospace.toml are generated by bootstrap
# but we track them so the repo works immediately after clone for the active profile.
# They will be regenerated when switching profiles or running bootstrap.

# AWS configs (credentials should never be committed)
.aws/credentials
.aws/config
